# Netlify deploy config for agency-site (Next.js 16 + Prisma + Neon)
# Docs: docs/netlify-deploy.md
#
# Publish directory: MUST be ".next" here (not repo root, not empty in UI).
# @netlify/config defaults publish to "." when unset → plugin fails:
# "publish directory cannot be the same as the base directory".
# UI Publish directory must be EMPTY (Clear); netlify.toml overrides the default.

[build]
  command = "npm run build:netlify"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  # Remote git builds failed with exit code 2: secret scanner flagged DATABASE_URL in build output (false positive).
  SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"
  SECRETS_SCAN_ENABLED = "false"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Prisma query engine must ship with serverless functions
[functions]
  node_bundler = "esbuild"
  external_node_modules = ["@prisma/client"]
  included_files = ["node_modules/.prisma/**", "node_modules/@prisma/client/**"]

# Upload routes: Next.js maxDuration export is respected by OpenNext where supported.
# Netlify sync functions cap at 60s; request body is limited to ~6 MB (see docs).
